import { styled, Table, TableContainer } from "@mui/material";


export const CustomTableContainer = styled(TableContainer)({
    height: 600
});

export const CustomTable = styled(Table)({
    minWidth: 400, height: 'max-content'
});