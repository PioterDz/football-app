import { styled, ToggleButton } from "@mui/material";

export const LoseBtn = styled(ToggleButton)({
    backgroundColor: 'red'
});
export const WinBtn = styled(ToggleButton)({
    backgroundColor: 'green'
});
export const DrawBtn = styled(ToggleButton)({
    backgroundColor: 'grey'
})